export default function NexusReservarPage() {
  return (
    <main className="flex min-h-screen items-center justify-center bg-sky-50 px-6 text-slate-800">
      <section className="w-full max-w-xl rounded-lg border border-sky-100 bg-white p-10 text-center shadow-sm">
        <p className="text-sm font-semibold uppercase tracking-[0.2em] text-sky-600">Nexus · Atención</p>
        <h1 className="mt-3 text-3xl font-semibold">Reservar una cita</h1>
        <p className="mt-3 text-slate-500">Inicio del proceso de reserva.</p>
      </section>
    </main>
  );
}
